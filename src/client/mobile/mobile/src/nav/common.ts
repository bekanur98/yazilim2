// navigation commons

import { COLORS } from "../constants";

export const defaultOpts = {
  headerTintColor: 'white',
  headerStyle: {
    backgroundColor: COLORS.b,
  },
  headerTitleStyle: {
    color: 'white',
  },
}

